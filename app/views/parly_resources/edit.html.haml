%h1 Editing parly_resource

= error_messages_for :parly_resource

- form_for(:parly_resource, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url